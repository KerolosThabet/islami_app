import 'package:flutter/material.dart';

class AhadethWidget extends StatelessWidget {
  const AhadethWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.redAccent,);
  }
}
